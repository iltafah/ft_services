apiVersion: 1

providers:
  - name: Default    # A uniquely identifiable name for the provider
    folder: 'ilias' # The folder where to place the dashboards
    type: file
    options:
      path:  /var/lib/grafana/dashboards